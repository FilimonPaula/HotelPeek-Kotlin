package com.example.kotlinhotelpeek.screens

import android.util.Log
import androidx.compose.foundation.Image
import androidx.compose.foundation.clickable
import androidx.compose.foundation.layout.*
import androidx.compose.foundation.lazy.LazyColumn
import androidx.compose.foundation.lazy.items
import androidx.compose.material.icons.Icons
import androidx.compose.material.icons.filled.ArrowBack
import androidx.compose.material.icons.filled.Settings
import androidx.compose.material3.*
import androidx.compose.runtime.*
import androidx.compose.ui.Modifier
import androidx.compose.ui.layout.ContentScale
import androidx.compose.ui.res.painterResource
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.dp
import androidx.navigation.NavController
import com.example.kotlinhotelpeek.R
import com.example.kotlinhotelpeek.models.HotelOffer
import com.google.firebase.auth.FirebaseAuth
import com.google.firebase.firestore.FirebaseFirestore
import com.google.firebase.firestore.Source
import kotlinx.coroutines.tasks.await

@OptIn(ExperimentalMaterial3Api::class)
@Composable
fun FavoritesScreen(navController: NavController) {
    val db = FirebaseFirestore.getInstance()
    val user = FirebaseAuth.getInstance().currentUser

    var hotels by remember { mutableStateOf<List<HotelOffer>>(emptyList()) }
    var isLoading by remember { mutableStateOf(true) }
    var error by remember { mutableStateOf<String?>(null) }

    LaunchedEffect(Unit) {
        try {
            user?.let {
                // Load from cache first
                val cachedResult = db.collection("users")
                    .document(it.uid)
                    .collection("favorites")
                    .get(Source.CACHE)
                    .await()

                val cachedHotels = cachedResult.documents.mapNotNull {
                    try {
                        it.toObject(HotelOffer::class.java)
                    } catch (e: Exception) {
                        Log.e("FavoritesScreen", "Cache parse failed: ${e.message}")
                        null
                    }
                }

                if (cachedHotels.isNotEmpty()) {
                    hotels = cachedHotels
                }

                // Then attempt to load from server
                try {
                    val result = db.collection("users")
                        .document(it.uid)
                        .collection("favorites")
                        .get(Source.SERVER)
                        .await()

                    val serverHotels = result.documents.mapNotNull {
                        try {
                            it.toObject(HotelOffer::class.java)
                        } catch (e: Exception) {
                            Log.e("FavoritesScreen", "Server parse failed: ${e.message}")
                            null
                        }
                    }

                    if (serverHotels.isNotEmpty()) {
                        hotels = serverHotels
                    }else{
                        //it shows error if not added
                    }

                } catch (serverException: Exception) {
                    Log.w("FavoritesScreen", "Server unavailable: ${serverException.message}")
                    // Silent fail, keep cached data
                }

            } ?: run {
                error = "User not logged in."
            }
        } catch (e: Exception) {
            Log.e("FavoritesScreen", "Load error: ${e.message}")
            error = "Failed to load favorites."
        } finally {
            isLoading = false
        }
    }

    Scaffold(
        topBar = {
            TopAppBar(
                title = { Text("Favorites") },
                navigationIcon = {
                    IconButton(onClick = { navController.popBackStack() }) {
                        Icon(Icons.Default.ArrowBack, contentDescription = "Back")
                    }
                },
                actions = {
                    IconButton(onClick = {
                        navController.navigate("Settings")
                    }) {
                        Icon(Icons.Default.Settings, contentDescription = "Settings")
                    }
                }
            )
        }
    ) { padding ->
        Column(
            modifier = Modifier
                .padding(padding)
                .padding(16.dp)
        ) {
            Text(
                text = "Your Favorite Hotels ❤️",
                style = MaterialTheme.typography.titleLarge,
                color = MaterialTheme.colorScheme.primary
            )

            Spacer(Modifier.height(16.dp))

            when {
                isLoading -> CircularProgressIndicator()
                error != null -> Text(text = error ?: "Unknown error")
                hotels.isEmpty() -> Text("No favorites yet.")
                else -> {
                    LazyColumn(verticalArrangement = Arrangement.spacedBy(12.dp)) {
                        items(hotels) { hotel ->
                            Card(
                                modifier = Modifier.fillMaxWidth(),
                                elevation = CardDefaults.cardElevation(6.dp)
                            ) {
                                Column(modifier = Modifier.padding(16.dp)) {
                                    Image(
                                        painter = painterResource(R.drawable.hotel_placeholder),
                                        contentDescription = null,
                                        modifier = Modifier
                                            .fillMaxWidth()
                                            .height(160.dp),
                                        contentScale = ContentScale.Crop
                                    )
                                    Spacer(Modifier.height(8.dp))
                                    Text(hotel.hotelName, fontWeight = FontWeight.Bold)
                                    Text(
                                        "View Details →",
                                        color = MaterialTheme.colorScheme.primary,
                                        modifier = Modifier.clickable {
                                            navController.currentBackStackEntry
                                                ?.savedStateHandle?.set("selectedHotel", hotel)
                                            navController.navigate("HotelDetails")
                                        }
                                    )
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}
